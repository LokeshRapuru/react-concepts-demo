function Home() {
  return (
    <section>
      <h2>React Concepts Playground</h2>
      <p>
        Use the nav above to explore each area: hooks + Context on one page,
        Redux on another, and a small multi-page demo showing React Router
        with URL params. This page itself is proof that routing works —
        it's rendered by <code>react-router-dom</code> based on the URL, with
        no full page reload when you click a nav link.
      </p>
    </section>
  )
}

export default Home
